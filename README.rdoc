== Test Project Two

I'll keep here fresh demos and thing I learn.

== Install

Bundle install.

    bundle install

Edit "config/database.yml".

Create DB tables.

    bundle exec rake db:create

Run migrations

    bundle exec rake db:migrate
